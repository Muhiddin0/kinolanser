import React from 'react';
import { SliderForBanner } from './slider';

type Props = {};

export const BannerSlider = (props: Props) => {
  return (
    <section>
      <SliderForBanner />
    </section>
  );
};
